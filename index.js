// Código JavaScript para adicionar comportamento aos botões

var closeButton = document.querySelector('.close-button');
var minimizeButton = document.querySelector('.minimize-button');
var zoomButton = document.querySelector('.zoom-button');

closeButton.addEventListener('click', function() {
    // Lógica para fechar o programa
});

minimizeButton.addEventListener('click', function() {
    // Lógica para minimizar o programa
});

zoomButton.addEventListener('click', function() {
    // Lógica para maximizar o programa
});
